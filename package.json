{
  "name": "pre-programmed",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate:assets": "node scripts/generate-asset-manifest.mjs",
    "prebuild": "npm run generate:assets",
    "build": "tsc -b && vite build",
    "prebuild:pages": "npm run generate:assets",
    "build:pages": "tsc -b && vite build --mode pages",
    "preview": "npm run build && vite preview",
    "deploy": "npm run typecheck && wrangler deploy",
    "pretypecheck": "npm run generate:assets",
    "typecheck": "tsc -b",
    "pretest": "npm run generate:assets",
    "test": "vitest run",
    "verify:pages": "npm run generate:assets && vitest run && tsc -b && vite build --mode pages",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260829.1",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "6.1.1",
    "typescript": "^5.9.0",
    "vite": "8.2.2",
    "vitest": "^3.2.4",
    "wrangler": "4.127.1"
  }
}
