name: Apply Commands reference source structured migration

on:
  push:
    branches:
      - author-ux-integration-refactor

permissions:
  contents: write

jobs:
  migrate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: author-ux-integration-refactor
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - name: Migrate Commands Reference Source editor
        run: python scripts/apply-command-reference-source-structured.py
      - name: Install dependencies
        run: npm ci --no-audit --no-fund
      - name: Verify repository
        run: npm run verify
      - name: Commit verified migration and remove one-shot tooling
        shell: bash
        run: |
          rm scripts/apply-command-reference-source-structured.py
          rm .github/workflows/apply-command-reference-source-structured.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/features/commands/author/structuredWorkspaces.ts src/features/commands/author/CommandSettings.tsx src/features/commands/author/commandSettings.css scripts/apply-command-reference-source-structured.py .github/workflows/apply-command-reference-source-structured.yml
          git commit -m 'refactor: structure commands reference source editor'
          git push origin HEAD:author-ux-integration-refactor
