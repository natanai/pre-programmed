name: Verify capture input repair

on:
  push:
    branches:
      - agent/capture-input-terminal-mode-repair-v2

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: echo ok
