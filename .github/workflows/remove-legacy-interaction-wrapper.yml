name: Remove legacy Interaction wrapper

on:
  push:
    branches:
      - author-ux-integration-refactor
    paths:
      - .github/scripts/remove-legacy-interaction-wrapper.py
      - .github/workflows/remove-legacy-interaction-wrapper.yml

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: author-ux-integration-refactor
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: python .github/scripts/remove-legacy-interaction-wrapper.py
      - run: npm ci --no-audit --no-fund
      - run: npm run verify
      - name: Commit verified cleanup and remove temporary tooling
        run: |
          rm .github/scripts/remove-legacy-interaction-wrapper.py
          rm .github/workflows/remove-legacy-interaction-wrapper.yml
          git add -A
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git diff --cached --quiet; then
            echo "No cleanup changes to commit"
            exit 1
          fi
          git commit -m "refactor: remove legacy interaction wrapper"
          git push origin HEAD:author-ux-integration-refactor
