# Hosted installation overrides.
# Local development continues to proxy /api to the local Worker.
VITE_API_ORIGIN=https://your-worker.your-subdomain.workers.dev

# GitHub Pages/project subpath for a fork or renamed repository.
# Keep leading and trailing slashes.
VITE_BASE_PATH=/your-repository-name/
