import type { CommandProjectSettings } from "./model";

/** Starter-project language contributed by currently installed features. */
export const DEFAULT_COMMAND_PROJECT_SETTINGS: CommandProjectSettings = {
  referenceSources: [],
  commands: [],
};
